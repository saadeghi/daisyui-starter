## TailwindCSS + DaisyUI boilerplate
##### using TailwindCSS JIT
##### with `autoprefixer` and `cssnano` for production

---

1. Clone the project
2. Install:
```
npm install
```
3. Generate CSS with:
```
npm run dev
```
```
npm run prod
```
